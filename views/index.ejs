<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Messages</title>
</head>
<body>
    <h1>All Messages</h1>
    
    <ul>
        <% for (let i = 0; i < messages.length; i++) { %>
          <li>
              <h2><%= messages[i].user %></h2>
              <p><%= messages[i].text %></p>
              <small><%= messages[i].added %></small>
          </li>
        <% } %>
    </ul>

</body>
</html>